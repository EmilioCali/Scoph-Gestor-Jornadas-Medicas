# workday-service

Jornadas médicas (CRUD, estados, médicos asignados).

- Puerto: **3021**
- Health: `GET /api/v1/health`
- Docs: `GET /api/docs`
- Env: `MONGO_URI` (no `MONGODB_URI`)

```bash
pnpm install
pnpm dev
pnpm test
```

Contexto: [docs/ARQUITECTURA.md](../docs/ARQUITECTURA.md).
